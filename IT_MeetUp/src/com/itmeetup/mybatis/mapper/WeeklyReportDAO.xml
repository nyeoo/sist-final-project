<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- mybatis 환경 설정 파일 활용 -->
<mapper namespace="com.itmeetup.mybatis.IWeeklyReportDAO">

	<select id="weeklyReport"
		resultType="com.itmeetup.mybatis.WeeklyReportListDTO">
		SELECT ROWNUM "countWeekly", TJ_OP_CODE "tjOpCode",
		WR_TITLE "wrTitle", WR_DATE "wrDate", PC_CODE
		"pcCode",PI_NICKNAME
		"piNickName", WR_CONTENT "wrContent"
		FROM VIEW_WEEKLY_REPORT
		WHERE TJ_OP_CODE = #{opCode}
		ORDER BY WR_DATE DESC
	</select>
	
	<select id="countWeeklyReport"
		resultType="int">
		SELECT COUNT(*)
		FROM VIEW_WEEKLY_REPORT
		WHERE TJ_OP_CODE = #{opCode}
	</select>

	

</mapper>